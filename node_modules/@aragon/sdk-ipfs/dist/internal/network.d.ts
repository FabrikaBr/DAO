import { IClientConfig } from "../interfaces";
import { GenericRecord } from "../typings";
export declare namespace Network {
    /**
     * Performs a request and returns a JSON object with the response
     */
    function request(config: IClientConfig, path: string, { method, params, body, signal, }: {
        method?: string;
        params?: GenericRecord;
        body?: BodyInit;
        signal?: AbortSignal;
    }): Promise<any>;
    function stream({ url, headers }: IClientConfig, path: string, { method, params, body, signal, }: {
        method?: string;
        params?: GenericRecord;
        body?: BodyInit;
        signal?: AbortSignal;
    }): AsyncGenerator<Uint8Array, void, unknown>;
}
