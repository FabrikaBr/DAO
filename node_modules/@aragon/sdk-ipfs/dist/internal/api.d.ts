import { AddOptions, CatOptions, IClientConfig, NodeInfoOptions, PinOptions, UnpinOptions, VersionOptions } from "../interfaces";
import { AddResponse, NodeInfoResponse, PinResponse, VersionResponse } from "../typings";
export declare namespace API {
    /** Gets cluster version */
    function version(cluster: IClientConfig, options?: VersionOptions): Promise<VersionResponse>;
    /** Gets the cluster node information */
    function nodeInfo(cluster: IClientConfig, options?: NodeInfoOptions): Promise<NodeInfoResponse>;
    /** Upload a file to the cluster and pin it */
    function add(cluster: IClientConfig, file: File | Blob | Uint8Array | string, options?: AddOptions): Promise<AddResponse>;
    /** Fetches the data behind the given path or CiD and returns it as bytes */
    function cat(cluster: IClientConfig, path: string, options?: CatOptions): Promise<Uint8Array>;
    /** Pins the given path or CiD or IPFS/IPNS path to the cluster */
    function pin(cluster: IClientConfig, path: string, options?: PinOptions): Promise<PinResponse>;
    /** Unpins the given path or CiD or IPFS/IPNS path from the cluster */
    function unpin(cluster: IClientConfig, path: string, options?: UnpinOptions): Promise<PinResponse>;
}
