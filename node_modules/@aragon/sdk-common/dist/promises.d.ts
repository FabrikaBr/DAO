/**
 * This function emulates the Promise.allSettled()
 *
 * @param proms
 */
export declare function allSettled(proms: Array<Promise<any>>): Promise<Array<{
    value: any;
    status: string;
} | {
    reason: Error;
    status: string;
}>>;
export declare function runAndRetry<T>({ func, onFail, shouldRetry }: {
    func: () => Promise<T>;
    onFail?: (e: Error) => void;
    shouldRetry: () => boolean;
}): Promise<T>;
