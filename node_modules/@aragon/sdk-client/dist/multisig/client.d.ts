import { IMultisigClient, IMultisigClientDecoding, IMultisigClientEncoding, IMultisigClientEstimation, IMultisigClientMethods } from "./interfaces";
import { ClientCore, ContextPlugin, PluginInstallItem } from "../client-common";
import { Networkish } from "@ethersproject/providers";
import { MultisigPluginInstallParams } from "./types";
/**
 * Provider a generic client with high level methods to manage and interact an Address List Voting plugin installed in a DAO
 */
export declare class MultisigClient extends ClientCore implements IMultisigClient {
    methods: IMultisigClientMethods;
    encoding: IMultisigClientEncoding;
    decoding: IMultisigClientDecoding;
    estimation: IMultisigClientEstimation;
    constructor(context: ContextPlugin);
    static encoding: {
        /**
         * Computes the parameters to be given when creating the DAO,
         * so that the plugin is configured
         *
         * @param {MultisigPluginInstallParams} params
         * @param {Networkish} [network="mainnet"]
         * @return {*}  {PluginInstallItem}
         * @memberof MultisigClient
         */
        getPluginInstallItem: (params: MultisigPluginInstallParams, network?: Networkish) => PluginInstallItem;
    };
}
