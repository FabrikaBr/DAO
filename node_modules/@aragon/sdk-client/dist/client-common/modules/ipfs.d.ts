import { Context } from "../../client-common/context";
import { IClientIpfsCore } from "../interfaces";
import { Client as IpfsClient, PinResponse } from "@aragon/sdk-ipfs";
export declare class IPFSModule implements IClientIpfsCore {
    private clientIdx;
    private clients;
    constructor(context: Context);
    getClient(): IpfsClient;
    /**
     * Starts using the next available IPFS endpoint
     */
    shiftClient(): void;
    /** Returns `true` if the current client is on line */
    isUp(): Promise<boolean>;
    ensureOnline(): Promise<void>;
    getOnlineClient(): Promise<IpfsClient>;
    add(input: string | Uint8Array): Promise<string>;
    pin(input: string): Promise<PinResponse>;
    fetchBytes(cid: string): Promise<Uint8Array | undefined>;
    private runAndRetryHelper;
    fetchString(cid: string): Promise<string>;
}
