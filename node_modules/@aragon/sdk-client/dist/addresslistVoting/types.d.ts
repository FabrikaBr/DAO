import { MajorityVotingProposalSettings, ProposalBase, ProposalListItemBase, VersionTag, VoteValues, VotingSettings } from "../client-common";
export declare type AddresslistVotingProposal = ProposalBase & {
    result: AddresslistVotingProposalResult;
    settings: MajorityVotingProposalSettings;
    votes: Array<{
        address: string;
        vote: VoteValues;
        voteReplaced: boolean;
    }>;
    totalVotingWeight: number;
};
export declare type AddresslistVotingProposalListItem = ProposalListItemBase & {
    result: AddresslistVotingProposalResult;
    votes: Array<{
        address: string;
        vote: VoteValues;
        voteReplaced: boolean;
    }>;
};
export declare type AddresslistVotingProposalResult = {
    yes: number;
    no: number;
    abstain: number;
};
export declare type AddresslistVotingPluginInstall = {
    addresses: string[];
    votingSettings: VotingSettings;
};
export declare type AddresslistVotingPluginPrepareInstallationParams = {
    settings: AddresslistVotingPluginInstall;
    daoAddressOrEns: string;
    versionTag?: VersionTag;
};
